#include <iostream>
using namespace std;
int main() {
    int n, sum = 0, product = 1;
    cin >> n;
    while (n > 0) {
        int d = n % 10;
        sum += d;
        product *= d;
        n /= 10;
    }
    if (sum == product) cout << "Spy";
    else cout << "Not Spy";
}
